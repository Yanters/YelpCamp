<link rel="stylesheet" href="/stylesheets/app.css">

<% layout('layout/boilerplate')%>

    <div id="map" class="mb-4"></div>
 
    <h1 class="mb-3">All Campgrounds</h1>
    <% if(campgrounds.length<10){ var end=campgrounds.length}else{end=10} %> 
    <%   for(var j=0;j<end;j++){ %> 
        <div class="card">
            <div class="row">
                <div class="col-md-4">
                    <img class="img-fluid" style="border-radius: 5px" alt="" src="<%= campgrounds[j].images[0].url %> ">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h5 class="card-title">
                            <%= campgrounds[j].title %>
                        </h5>
                        <p class="card-text">
                            <%= campgrounds[j].description %>
                        </p>
                        <p class="card-text">
                            $<%= campgrounds[j].price %>/day
                        </p>
                        <p class="card-text">
                            <small class="text-muted">
                                <%= campgrounds[j].location %>
                            </small>
                        </p>
                        <a href="/campgrounds/<%= campgrounds[j]._id %>" class="btn btn-primary">View <%= campgrounds[j].title
                                %> </a>
                    </div>
                </div>
            </div>
        </div>
    <% } %> 
    <script>
       var indexNumber = <%= j  %>;  
    //    console.log(indexNumber);
       var CampgroundsSize = <%= campgrounds.length  %> 
    //    console.log(CampgroundsSize);
    </script>
            <script>
                const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
                const campgrounds = { features: <%- JSON.stringify(campgrounds) %>};
                
                </script>
            <script src="/javascripts/clusterMap.js"></script>
            
            <%- include('../partials/loadCampgrounds') %> 